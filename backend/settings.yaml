# MB
maxUploadSize: 512
language: en
model:
  # tiny / base / small / medium / large-v2 / large-v3
  size: tiny
  # int8 / int8_float16 / float16 / bfloat16 / float32
  compute_type: float16
  # cpu / cuda / auto
  device: cuda
